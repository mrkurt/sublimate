require 'spec_helper'

describe Sublimate::Uploader do
  it "uses direct upload for google" do
#    uploader = get_google_store
#
#    uploader.store_file(tmpfile, :bucket => 'tmp')
#    bucket = uploader.storage.directories.get('tmp')
#    g = bucket.files.head(tmpfile)
#    g.content_length.should == File.size(tmpfile)
  end

end
